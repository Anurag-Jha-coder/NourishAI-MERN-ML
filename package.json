{
  "name": "nourish-ai-mern",
  "version": "1.0.0",
  "description": "Personalized Diet Recommendation System - MERN + Python ML",
  "scripts": {
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "dev": "concurrently \"npm run ml\" \"npm run server\" \"npm run client\"",
    "ml": "cd ml && python app.py",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "train": "cd ml && python generate_dataset.py && python train_models.py",
    "build": "cd client && npm run build",
    "start": "concurrently \"npm run ml\" \"npm run server\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
